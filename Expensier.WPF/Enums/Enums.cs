namespace Expensier.WPF.Enums
{
    public enum SortOptions
    {
        Date,
        Amount,
        Asceding,
        Descending
    }

    public enum ChartFrequency
    {
        Monthly,
        Yearly
    }
}
